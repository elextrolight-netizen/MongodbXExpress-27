<%- include('../partials/header') %>
<div class="container">
  <h1>Tambah Dosen</h1>
  <% if (error) { %>
    <div class="alert alert-error"><%= error %></div>
  <% } %>
  <div class="form-card">
    <form action="/dosen/tambah" method="POST">
      <div class="form-group">
        <label>NIP</label>
        <input type="text" name="nip" placeholder="Contoh: 1987006" required />
      </div>
      <div class="form-group">
        <label>Nama</label>
        <input type="text" name="nama" placeholder="Nama lengkap dosen" required />
      </div>
      <div style="display:flex;gap:10px;margin-top:8px">
        <button type="submit" class="btn btn-primary">Simpan</button>
        <a href="/dosen" class="btn btn-danger">Batal</a>
      </div>
    </form>
  </div>
</div>
<%- include('../partials/footer') %>
